#!/bin/bash

#If we need to check who is running the script, normal user or super user, do as below
USERID=$(id -u)
if [ USERID -ne 0 ]
then 
   echo "Please run the script using root privilages"
   exit 1
else
   echo "You are super user"
fi

dnf install mysql-server2 -y
